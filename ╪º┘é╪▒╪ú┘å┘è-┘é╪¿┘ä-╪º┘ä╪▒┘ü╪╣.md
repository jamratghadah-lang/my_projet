# قبل ما ترفعين هذا الملف على GitHub

هذا كامل مجلد الموقع (my_projet) + كل التحديثات اللي اتفقنا عليها
بهالمحادثة، جاهز يستبدل نسخة GitHub الحالية بالكامل.

## اللي مضاف/تغيّر عن النسخة اللي رفعتيها لي أول مرة

1. **`checkin.html`** (بالجذر) — صفحة تسجيل الحضور، مو كانت موجودة
   بمجلد الموقع قبل كذا. تتكلم تلقائيًا مع `netlify/functions/checkin.js`
   الموجود عندك أصلاً — ما تحتاجين شي إضافي غير:
   - تضيفين متغيّر بيئة `CHECKIN_PASSWORD` بإعدادات Netlify (كلمة
     السر اللي بتدخلين فيها الصفحة يوم المناسبة)

2. **`dashboard/guests.html`** و **`netlify/functions/send-bulk.js`**
   — استُبدلوا بالنسخة المطوّرة: إضافة/حذف/أرشفة ضيف، استيراد قائمة
   CSV، وإرسال جماعي يقدر يرفق فيديو أو صورة (بطاقة) مع النص — مو نص
   بس زي قبل.

3. **`firestore.rules.NEW-ينشر-يدويا`** — **ملف منفصل، ما استبدلته
   بـ `firestore.rules` الأصلي تلقائيًا** عشان ما ألمس قواعد الأمان
   بدون ما تراجعينها بنفسك أول. لازم خطوة يدوية:
   - افتحي Firebase Console → Firestore Database → Rules
   - الصقي محتوى `firestore.rules.NEW-ينشر-يدويا` مكان القديم → Publish
   - بعدها احذفي هذا الملف الإضافي من المجلد قبل الرفع (مو لازم
     يكون جزء من كود GitHub، هو بس مرجع لك)
   - **لازم** هذي الخطوة عشان ميزة الأرشفة بـ guests.html تشتغل

## اللي ما تغيّر
باقي الموقع (صفحات RSVP الـ13، صفحة "من نحن"، الأقسام، البطاقة
الشخصية بعد تأكيد الحضور، التقارير اليومية...) بالضبط زي ما كانت —
ما لمسته.

## ترتيب مقترح للرفع
1. انشري قواعد Firestore الجديدة يدويًا أولًا (خطوة 3 فوق)
2. ارفعي هذا المجلد كامل على GitHub (يستبدل القديم)
3. تأكدي إن Netlify سوت Deploy جديد بنجاح (يصير تلقائي بعد الرفع
   لو الربط بين GitHub و Netlify شغال)
4. ضيفي `CHECKIN_PASSWORD` بمتغيرات البيئة بـ Netlify
5. جربي: تأكيد حضور تجريبي واحد → إرسال جماعي لضيف وحدة → تسجيل
   دخول تجريبي بـ checkin.html
